# ---- Core scientific stack (pinned to your working versions) ----
numpy==2.3.2
pandas==2.3.2
scipy==1.16.1
scikit-learn==1.7.1
matplotlib==3.10.6
seaborn==0.13.2

# File formats / IO
pyarrow==21.0.0

# Model libs
xgboost==3.0.4
lightgbm==4.6.0
statsmodels==0.14.5

# Utils
tqdm==4.67.1
rich==14.1.0
joblib==1.5.2

# ---- Jupyter (optional, for notebooks) ----
jupyter==1.1.1
ipykernel==6.30.1

# ---- ICL / Foundation models ----
# PyTorch (CPU wheels are on PyPI; if you want GPU, see note below)
torch>=2.2,<2.5
# TabPFN (tabular in-context learner; pulls einops etc. as deps)
tabpfn>=0.1.10
einops>=0.7.0

# ---- Optional: speeds up model artifact fetches (used by TabPFN) ----
huggingface_hub[hf_xet]>=0.24.0
